collections:
  members:
    output: true
  published:
    output: true

defaults:
  - scope:
      path: ""
      type: "members"
    values:
      layout: "member"

exclude: 
  - tmp/
  - vendor/

plugins:
  - jekyll-feed
  - jekyll-sitemap
  - jekyll-seo-tag